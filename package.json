{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && next dev -p 5000",
    "dev:remote": "npm run build && heroku local",
    "build": "next build",
    "start": "npm run build && next start -p 5000"
  },
  "dependencies": {
    "next": "^11.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
